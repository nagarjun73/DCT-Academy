const square = (a) => {
  return a * a
}

console.log(square(5));

//shorter version of arrow function
//only one arameter opening and closing brackets are optional
//of only one statement {} bracket are optional and return is not needed but statement is written in same line as => symbol
const square2 = (a) => a * a

console.log(square2(5));